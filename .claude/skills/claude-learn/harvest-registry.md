# Harvest registry

Procedures a session proved deterministic, and how far each has been taken.
Owned by [`references/determinism-harvest.md`](references/determinism-harvest.md) (task 2b).

Status: `candidate` (deterministic, not scripted yet) -> `scripted` (parameterized script, reproduced
the manual result once) -> `tested` (an automated check asserts its summary line).
`Proven on` names the real case the script was run against. A single successful run is `scripted`,
never `tested`.

| Procedure | Skill | Script | Status | Proven on | Test |
|---|---|---|---|---|---|
| Seed and bring up a whole blue/green slot on a Hetzner box from the stack spec: build each app with its own build args, stream it over ssh, recreate the container, verify the slot | `deploy-hetzner-box` | `scripts/seed-slot.sh` (with `seed-image.sh`, `deploy-slot.sh`, `verify-ports.sh`) | scripted | media-creation-team blue on integrations-ai: 6 apps, skipped=6 up=6 failed=0 on the idempotent re-run, and it caught a real spec/`APPS` disagreement the manual run had not | none |
| Azure invoice PDFs + totals + credit-funded flag for a date range | `azure` | `scripts/fetch-azure-invoices.sh` (+ `scripts/parse-azure-invoices.py`) | scripted | 2026-01-01..2026-12-31, 3 subscriptions, 23 invoices, 16 bookable, 606.11 EUR | none |
| Book an Azure invoice manifest into a vouchers-ai instance | vouchers-ai repo | `infra/scripts/book-azure-invoices.mjs` | scripted | local DB and prod green, 16 vouchers + 16 archived PDFs each, re-run skipped all 16 on both | none |
| Onboard a repo to the worktime service: customer and repo rows, `worktime` in `.mcp.json` on 5211, empty `.time/working-time.json`, scoped first import | `import-sow` | none yet | candidate | done by hand twice, for `vouchers-ai` and `integrations.at` | none |
| Turn a seed JSON document into a guarded, idempotent SQL import delta: DB_NAME guard, schema and CHECK-constraint preconditions, one `INSERT ... FROM (VALUES ...) WHERE NOT EXISTS` per table in dependency order, a closing row-count assertion that rolls back on a mismatch | media-creation-team repo | none yet | candidate | the emission shape is proven by `db/video-editor-mcp/generate-ai-at-work-import.py`, but that script is content-specific to one video and reads a HeyGen export rather than a seed document; the generic seed-document form has not been written | none |
| Additive SQL delta for rows in a table a generator already owns (any repo): manifest in, guarded idempotent scoped script out | `dotnet-conventions` | `scripts/gen-additive-delta.py` (+ a per-repo manifest producer, here `integrations.at`'s `db/api.integrations.at/generate-capability-additions.py`) | scripted | 7 capability items on dev, blue and green: 7 items, 7 memberships, 63 list entries, 9 use cases; scoped counts all matched and the four table totals were unchanged by the re-run | none |
| Verify an HTTP MCP server end to end: stateless `tools/list`, assert the exact tool set, call named tools and assert a non-empty payload; for a **local container** also assert docker reports it healthy, `/health` answers 200 with no credentials, and `/mcp` 401s without a key | `dotnet-conventions` | `scripts/verify-mcp-server.sh` | scripted | `integrations.at` public server on `localhost:5089/mcp`: 5 tools matched the expected set, `list_classes` returned 10, `find_capabilities` returned 55. **Extended 2026-08-28** with `--health`, `--unauth-401` and `--container` after those three checks were run by hand against three freshly built containers: `customers-mcp` 12 tools, `customers-secrets-mcp` 4, `worktime-mcp` 12, all healthy, all 200 anonymous on `/health` and 401 unauthenticated on `/mcp`. Written because a near-duplicate script had already been started elsewhere before the registry was read | none |
| Render a booked voucher as the branded invoice PDF, optionally with a markdown appendix from page 2 (identity, texts and badge blobs resolved from the DB and blob storage, not hardcoded) | `vouchers-ui-design` (repo-local, `references/invoice-preview.md`) | `.claude/skills/vouchers-ui-design/scripts/build-invoice-pdf.py` | scripted | `023-2026` with a 49-row time ledger appended: 5 lines, net 5098.00, tax 1019.60, gross 6117.60, 5 pages, extracted text identical to the hand-built delivery | none |
| Repair non-ASCII text a codepage-mangling client wrote into SQL Server, and prove the repair by code point (ASCII-only hex writes, `LEN`/`UNICODE` asserts) | `sql-server` (`references/sqlcmd-safe-execution.md`) | none yet | candidate | 5 `VoucherEntries.Remark` values + `InvoiceTemplates.BankName` in `vouchers-kastil`, 2026-08-17: 5/5 entries and the template field verified at U+00E0 / U+20AC | none |
| Local <-> global skill drift inventory: classify every skill synced/diverged/one-sided, then per file count the lines that exist on only one side so a Pull that would delete local content is caught before it runs | `claude-learn` (`references/skill-sync.md`) | `scripts/skill-drift.sh` | scripted | `bauer-sport`, 2026-08-17: reproduced the hand-rolled inventory of 32 local skills, and the drill-down on `angular-conventions` and `dotnet-conventions` matched the manual per-file counts that had just decided the merge | none |
| Overwrite a local dev database with live prod off the shared SQL box: version gate, `COPY_ONLY` backup, `scp` from the docker volume, stage to `InstanceDefaultBackupPath`, `RESTORE WITH REPLACE, MOVE` onto the dev instance's existing paths, orphaned-user repair | `sql-server` (`references/refresh-dev-from-box.md`) | none yet | candidate | `vouchers-kastil` -> `Vouchers-Dev`, 2026-08-17, 31 tables restored; logical names and dev file paths are the only per-run inputs | none |
| Angular update compatibility gate across every workspace in a repo: installed vs latest core, TypeScript against the `@angular/compiler-cli` peer range, `engines.node` against both the running node and the Dockerfile base tag (flagging a floating tag), the MSAL README support matrix, and every unmet `@angular/core` peer with its dist-tags | `angular-conventions` (`references/angular-update.md`) | `scripts/angular-update-preflight.mjs` | scripted | `bauer-sport`, 2026-08-17: 4 workspaces, reproduced the post-update state (3 warn on the `@ngrx/signals` unmet peer, TS in range, node floor met, images pinned); a fixture pinned at msal-angular 5.3.0 + `FROM node:22` reproduced both defects the session found by hand, BLOCKED with exit 1 | none |
| Roll per-slot MCP settings onto a box CI never touches: per-key upsert (never whole-file copy, because the box holds real passwords where the repo holds placeholders), wire the key into a compose `environment:` whitelist, assert no placeholder survived, then recreate the API services under the shared deploy lock | `mcp-claude` / `deploy-hetzner-box` | `docs/claude-mcp/apply-mcp-env-on-box.sh` (bauer-sport) | scripted | bauer-sport box, 2026-08-17: 5 env files across 3 apps, citythong had no MCP keys at all and VFN had no `Mcp__ResourceUri`; 5 containers recreated, placeholder check clean, all 6 endpoints green afterwards | none |
| Verify every MCP endpoint of an estate: anonymous 401, `WWW-Authenticate` advertising **https** not http, PRM `resource` equal to the host dialled, keyed `tools/list` returning the expected tool count, wrong key rejected | `mcp-claude` | `docs/claude-mcp/verify-mcp-endpoints.sh` (bauer-sport) | scripted | bauer-sport, 2026-08-17: run BEFORE the rollout it failed exactly the 6 known-broken checks of 30 and passed the rest, which is what made the later 30/30 ALL GREEN meaningful; it also caught a wrong assertion of my own (two hostnames on one container can never both pass the resource check) | none |
| Prove a chart-of-accounts classifier end to end against a live API: enumerate what already occupies the target number range, renumber/create the block through the app's own endpoint so its write guard is exercised, then book one receipt over `/mcp` and assert the aggregate it must move and the aggregates it must not | `austrian-tax` / `dotnet-conventions` (`references/dotnet-mcp.md`) | none yet | candidate | `Vouchers-Dev`, 2026-08-18: 8 accounts renumbered into 9500-9620 and 5 created, then `add_voucher` at gross 7.18 booked `VoucherType 1` at 0% VAT, `get_period_summary` unchanged at 7125.31 while `get_expenses(scope: Private)` reported 7.18 over 1 entry | none |
| Field-level rewrite and write-back of Hugo front-matter prose: extract every prose scalar with role-based stable ids, anchor it to exact source lines, gate with a byte-identical identity round-trip, write back in place without re-serializing the YAML, and propagate a changed card title key-scoped across page title, banner, `data/menu.json` and sibling sidebars | `optimize-page` command + `.claude/agents/cw-*.md` (integrations.at) | `.content-team/units.py` (+ `roundtrip_test.py`, `apply.py`, `export.py`, `rekey.py`) | scripted | `integrations.at`, 2026-08-18: identity round-trip byte-identical across all 10 `ai-at-work` files (325 units), 214 fields written back and later reverted on the owner's call with the diff confined to 9 content files; `CONTENT_DIR` re-pointed at `enterprise-ai` found 148 fields with no code change | none |
| Copy gate for a row of sibling fields: shared absolute word band with the rendered spread, banned vocabulary matched on word boundaries for single words and as substrings for phrases, em dashes, and required must-survive sentences that a rewrite would otherwise drop silently | `optimize-page` command (integrations.at) | `.content-team/lib/gate.py` (library and CLI) | tested | `integrations.at`, 2026-08-18: gated 5 pages of card bodies, reproducing the inline checks written by hand once per page (rows 434/426/424/434/443, spreads 0.7 to 5.8 percent) | 6-case negative test: fires on a banned word, a banned phrase, an em dash, an over-band count and a missing required sentence, and stays silent on "enterprise", the ent-ERP-rise false positive that motivated the word-boundary rule |
| Write text back into Hugo front matter without corrupting the file: detect each file's own newline (they differ inside one directory), refuse a mixed-ending file, refuse an empty replacement needle, assert a unique match per replacement, re-parse the front matter afterwards, and route inline scalars to `replace_once` against block scalars to `replace_block_scalar` | `optimize-page` command (integrations.at) | `.content-team/lib/writeback.py` | tested | `integrations.at`, 2026-08-18: written AFTER a corruption it would have prevented. The hub `_index.md` is CRLF and all six detail pages are LF; splitting on a hard-coded CRLF made the computed old block an empty string and `str.replace("", new, 1)` PREPENDED three card bodies to the top of the file. Restored from the HEAD blob, then all six pages rewritten through this module with zero further incidents | identity round-trip byte-identical on all 6 block-scalar files and correctly RED when fed deliberately altered text; `_index.md` reported as SKIP (inline scalars) rather than crashing |
| Hub-card write-back on the INLINE scalar path: identity round-trip against the file's own current text, per-card title and body swap asserted unique by title, refusal of any body containing a double quote, front matter re-parsed after the write | `optimize-page` command (integrations.at) | `.content-team/lib/writeback.py` (`identity_roundtrip_inline`, `replace_card_inline`, `apply_hub_cards`) | tested | `integrations.at` `/enterprise-ai/`, 2026-08-18: six hub cards written in one pass, CRLF preserved, `git status` immediately after showed one content file and exactly 12 insertions and 12 deletions. The module already routed hubs to `replace_once` but had no identity gate on that path, so every hub rewrite hand-rolled the same three steps | on a copy of the real hub: identity round-trip True, a no-op rewrite byte-identical, and both guards fire (unknown card title, and a double quote in the body) |
| Copy gate additions for a vendor-terminology and register pass: retired product names as substrings, first person anchored on word boundaries, row spread as a percentage, and a restatement report that deliberately returns NO verdict | `optimize-page` command (integrations.at) | `.content-team/lib/gate.py` (`RETIRED`, `FIRST_PERSON`, `spread`, `restatement_report`) | tested | `integrations.at` `/enterprise-ai/`, 2026-08-18: the six shipped cards pass silently under the enterprise profile (banned vocabulary lifted, band 42-46, spreads 2.2 and 4.7 percent) | 7-case negative test, one per class: retired name, first person, em dash, band, banned phrase, banned word, missing required sentence. The first-person case FAILED to go red on the first run, because a heredoc had written literal backspace bytes into the pattern list; the negative test is the only reason that shipped fixed |
| Prove a multi-page CSS change across a whole static site: read the sitemap, render every URL in a hidden iframe at each viewport, and assert per page that a component's content box stays inside its band, that its text does not wrap out of its own box, that its height matches the expected value, and that nothing scrolls horizontally | `hugo-design` (`references/hugo-neutral-banner.md`) | `.claude/skills/hugo-design/scripts/banner-crawl.py` | scripted | `integrations.at`, 2026-08-19: emitted JS reproduced the hand-run crawl exactly across 123 sitemap URLs at 1440/768/375, 78 pages matched, 0 clipped, and the same height distributions. Run BEFORE the two fixes it justified, it was the only thing that saw the 5 pages clipping 8px under a fixed `height` and the 4-line title wrap from a `16ch` cap tuned on a 10-character page. **Extended 2026-08-21** with `--title-selector` and a Range-based `wrappedCount`, which is now part of the pass condition: the hand-rolled version of this run derived line counts from element height minus a 27px constant for the `::after` rule, and reported eleven wrapped titles that were all single lines, because that block is suppressed on `.banner-title-row` pages and the spacing token changes at a breakpoint. After the fix it returns `wrappedCount: 0` across all 76 neutral banners at all three viewports | none |
| Turn a page of copy into installed house-consistent card illustrations: read the destination folder's existing assets to fix medium/ground/pixel size before prompting, generate candidates through the self-hosted media-creator MCP against a model roster with per-model resolution ceilings, retrieve the PNG off the host side of the container bind mount, centre-crop to the folder's exact ratio and re-encode to WebP, then grep the content tree and repoint only the one reference that uses it | `media-generation` + `hugo-design` (`references/hugo-create-image.md`) | `.claude/skills/media-generation/scripts/install-media.py` | scripted | `integrations.at`, 2026-08-19: 10 cards installed across `/enterprise-ai/`, its Claude, platform-decisions and agentic-devops pages, all 800x600 WebP at 8.6-30.8 KB, all 10 verified 200 from the dev site; `export_media` proved unusable from the repo (container cannot see `D:\`) and three shared asset files were left untouched by the grep-first rule. **Scripted 2026-08-22** with a `--mode cover|pad` switch, after nine class-hero photographs were installed by hand: the existing fit-and-pad path is right for diagrams and wrong for photographs, which bleed, so trimming their white border does nothing and padding boxes them in white inside a card slot built to fill. `--dry-run` on the Angular render reports `mode=cover src=1200x896 scaled=804x600 canvas=800x600`, matching the hand-run crop exactly | none |
| Verify one Hugo detail page against the section-layout contract: per-file line endings, the `banner.style` key, description band, every step's `group` against the declared `stepGroups` ids (a mismatch is SILENTLY DROPPED by the partial's `where` filter), any group holding exactly one step (the partial hardcodes the plural and renders "1 steps"), `type: plugin` consistency, stray `challenge`/`outcome` keys, the retired `gridStepsWithBanner`, the mandatory sidebar, and the card row's word counts and spread | `optimize-page` command (integrations.at) | `.content-team/lib/verify_page.py` | tested | `integrations.at` `/enterprise-ai/`, 2026-08-18: run on all five detail pages, green on the three written to the contract | **Found a real defect within a minute of being installed**: on the page that had just been rewritten, group `educate` held exactly one step and the live page was rendering "1 steps". Confirmed in the served HTML, fixed by rebalancing to 2/4/3 rather than editing the template, and re-verified at zero occurrences with all nine steps still rendered |
| Regression-test a PreToolUse guard hook against a corpus of real commands: one decision per line, optional per-line expectation, non-zero exit on any mismatch | `context-engineering` (`references/guard-hooks.md`) | `scripts/hook-cases.sh` | tested | `integrations.at`, 2026-08-20: 27 cases against `.claude/hooks/shell-guard.sh` (15 allow, 9 deny, 3 ask) all matched their expectation, reproducing by hand the three over-fires the guard had shown live (a `>` inside a quoted regex, the `>` of `=>`, and `> /dev/null && echo`) | inverted corpus: every expectation flipped, reported 15 MISMATCHED and exited 1 |
| Diff the capability catalogue a live blue/green slot serves against the dev database, then apply named SQL deltas to named slots in a fixed dev->blue->green order with count verification, a COMMIT-to-ROLLBACK dry run and a stop-before-green on any failure | `db-slot-sync` (repo-local, integrations.at) | `.claude/skills/db-slot-sync/scripts/check-catalogue-parity.py` + `scripts/apply-deltas.sh` | scripted | `integrations.at`, 2026-08-20: the parity script replayed against the pre-fix blue payload reported exactly the 18 differences derived by hand (16 group SortOrder/Icon rows plus the two `relationship-network-explorer` memberships), and `diffs=0` on both slots after the fix; the apply script re-ran both 2026-08-20 deltas on dev as a clean no-op and dry-ran them against blue and green, ignoring the caller's flag order to run blue before green | none |
| Preflight an offering sidecar before the importer sees it: every string-list field actually a list of strings, the three mechanism fields non-empty, `status` and `catalogue.action` inside their closed vocabularies, and every `extends` slug resolving against the published catalogue | `import-sow` (integrations.at) | `.claude/skills/import-sow/scripts/check-sidecar.py` | tested | `integrations.at` + `vouchers-ai`, 2026-08-20: written after an `evidence` object threw a `JsonException` and lost the whole harvest phase for 13 entries. Run across both ledgers it immediately found 16 further defects nobody had seen: seven entries carrying a full mechanism but no `status` and no `catalogue`, so `find_offering_candidates` never returns them and the work is invisible to a catalogue pass; one `catalogue.action: "candidate"`, which is not a value; and one status string invented that morning | 6-case negative corpus, one per class: nested-object `evidence`, a non-string list entry, an empty `mechanism.returns`, an unresolvable `extends` slug, an invented status and an invented action. All six fire, exit 1 |
| Apply a schema delta to every database an app owns, in the order test, dev, blue, green, with a per-slot verification block and a hard stop before production on any non-zero exit | `db-slot-sync` (integrations.at) | `.claude/skills/db-slot-sync/scripts/apply-deltas.sh` | tested | `integrations.at`, 2026-08-20: the approval-gate delta reached all four databases through it, 55 Approved on dev, blue and green and 48 on the test database, and a re-run on dev moved nothing. The `test` slot was added in this run after the suite failed 15 tests on `Invalid column name`: the script previously knew only the three slots that have a website in front of them | re-run on dev after the delta was edited: exit 0, all three RowsFound/RowsExpected lines matched, distribution unchanged, which is the idempotency check the skill asks for |
| Patch a snippet in a Caddyfile shared with another tenant's stack: dated backup, candidate generated from the live file and diffed before it is written, written in place so the file bind-mount's inode survives, `caddy validate` then `caddy reload`, then a status sweep of every hostname on the box including the co-tenant's, with the expected code stated per path | `deploy-hetzner-box` (`references/docker-compose-caddy.md`) | none yet | candidate | `integrations.at`, 2026-08-20: opening `GET /api/catalogue/detail/*` in `integrations_api_guard`. The diff was exactly the two intended blocks, host and container md5 matched before the write so the mount was proven attached, and the post-reload sweep held: blue detail 200, `/groups` and `/items` 403, POST 403, and all seven hostnames including `vouchersai.integrations.at` still serving | none |
| Orphan and broken-link sweep over a built static site: resolve every internal href to a file, count target pages with zero inbound links from any other page, and skip alias redirect stubs so the number is readable | `agentic-seo` (`references/audit-technical.md`) | `scripts/orphans.py` | scripted | `integrations.at`, 2026-08-20: reproduced the hand-written sweep that found 51 of 55 capability detail pages orphaned before the fix, and after it reported 1 orphan and 0 broken links across 6626 links. The alias filter is what makes it usable: without it the same clean site reports 59 orphans, all of them Hugo redirect stubs | none |
| Measure a rendered layout when the MCP browser profile is locked by another session: scratch-profile Chrome on its own debugging port, CDP `Runtime.evaluate` for a setup expression then a measurement expression returning JSON, plus a screenshot, swept over one parameter to turn a look into a delta | `chrome-devtools` | `scripts/cdp-measure.mjs` | scripted | `integrations.at`, 2026-08-20: the Customers Agent panel at three heights (560, 760, capped 820). Panel +200 gave chips +50 and request +150, panel +60 gave +15 and +45, proving the 1:3 surplus split exactly; the same run caught nothing because the two defects had already been fixed by hand, which is why it exists | none |
| Apply a hand-written SQL delta to a database whose connection string lives in an `appsettings.json`, without a credential ever reaching the shell: parse the file with `ConvertFrom-Json`, the string with `SqlConnectionStringBuilder`, hand the properties to `sqlcmd -i`, then re-run the same script to prove the already-applied guard | `sql-server` (`sqlcmd-safe-execution`) | none yet | candidate | `WorkTime`, 2026-08-20: the `dbo.WorkSession.Component` delta. Done by hand after a `grep`/`cut` extraction returned a 21-character string for a 19-character password and failed as `Login failed for user`, which cost a detour through the container's connection string and a search for a second SQL instance | none |
| Revert a pathspec on a shared checkout where `git restore`/`reset`/`stash` are refused by a hook: enumerate the porcelain code per file, write back the `HEAD` blob or the *staged* blob per file, and move index-only new files out of the tree instead of deleting them | `git-history-ops` | `scripts/safe-revert.sh` | tested | `integrations.at`, 2026-08-20: written after a hook refused `git restore` on `src/integrations.at` and I reported another session's nine staged files as unrecoverable. They were all in the index; `git show :<path>` returned every one verbatim, including an item-URL scheme the user believed was already deployed | 4-code corpus in a throwaway repo (`MM`, ` M`, ` D`, `A `): dry run modifies nothing; `--to index` takes the staged blob for `MM` and falls back to `HEAD` where no distinct staged version exists; a deleted file comes back; the `A ` file is copied to the rescue directory and removed from the tree, never deleted outright; the re-run reports it as already out of the tree rather than re-rescuing |
| Check a Hugo content file against the mechanical half of the house voice before reporting it done: every title and heading against the render cap, any negation or refusal wording in a title, and any body field that opens on a failure or a strawman | `brand-integrations` (`references/brand-voice.md`) | `.claude/skills/brand-integrations/scripts/voice-lint.py` | scripted | `integrations.at`, 2026-08-21: written after three pages of card and step copy took eight rounds of the owner quoting back the same three defects. Run over the four pages of that session it reports 0 findings on the three that were reworked and 11 on the one where only a single card was in scope, including the 61 and 62 character titles, four `X, Not Y` titles and the two failure-first openings still there | none |
| Which images a static site actually uses, bucketed rendered / referenced-only-by-retired-pages / dead, with the git-add date on every dead file | `optimize-images` | `scripts/asset-audit.py` | scripted | `integrations.at` Hugo site, 125 files: 122 rendered, 3 retired-only, 0 dead, reproducing the hand audit that had found 27 dead out of 151 | none |
| Prove a change altered nothing a reader can see: render both builds, compare the visible text of every page after stripping script/style/tags and unescaping entities, and assert the two page sets are identical so a partial build cannot pass by omission | `agentic-seo` (`SKILL.md` "Bundled scripts", `references/on-page.md`) | `.claude/skills/agentic-seo/scripts/visible-diff.py` | tested | `integrations.at`, 2026-08-21: written after I read a "change nothing in the UI" brief as "change nothing in the layout" and rewrote the homepage hero plus eleven meta descriptions. `legal-layout.html` prints `.Description` as the compliance masthead and `banner.html` falls back to it for the banner subtitle, so nine pages changed on screen; the hand-run version of this diff is the only thing that found the full set, and it later carried the verification receipt for the whole session | both directions: `build6` vs `build7` (identical trees) returns PASS at exit 0; the pre-change build vs the final one returns FAIL, 19 changed pages and 1 ADDED, matching the hand-derived list exactly |
| Detect a branch of a static site that no crawler and no reader can enter: count anchors per page with the quote-optional href regex that survives `--minify`, resolve absolute hrefs back to routes so a production `--environment` build still scans, report how many links were examined so a zero-denominator pass is visible, and flag pages whose only inbound links come from their own descendants | `agentic-seo` (`references/audit-technical.md`) | `.claude/skills/agentic-seo/scripts/orphans.py` (hardened) | tested | `integrations.at`, 2026-08-21: the previous version cleared a site where all 110 capability anchors sat inside `display: none` and `/capabilities/` had no inbound link except its own 55 children's breadcrumbs, and separately returned `broken internal links: 0` out of zero links examined on a `--minify` build. The hardened script reports `internal links examined: 3612` and names `/capabilities/ <- 55 descendant pages` as sealed, which is the finding the owner had to point out by hand | run against both a plain and a `--minify` build of the same tree: 3612 vs 3613 links examined, same 1 orphan, same sealed branch, `broken internal links: 0` on both, where the old script found nothing on the minified one |
| Prove a page retirement or a schema move did not silently delete structured data: list every JSON-LD block type per page across two clean builds, expand `@graph`, carry `numberOfItems` on an `ItemList`, and print only the pages that differ | `agentic-seo` (integrations.at) | `.claude/skills/agentic-seo/scripts/jsonld-diff.py` | scripted | `integrations.at`, 2026-08-21: the unparameterized original reported exactly 2 changed pages of 167 when `/capabilities/` was retired and its 55-item `ItemList` moved to the homepage, which is what proved the move rather than a deletion; the harvested version reproduces the same signature build and returns `0 of 166` on an identity run | none |
| Move a Hugo page from one section to another without losing a route: `mv` the file, rewrite its `url:`, prepend every old path to its own `aliases:`, then enumerate every surviving reference to the old route across `content/`, `data/` and `layouts/` as a work list, and after the edits assert the retired routes build as redirect stubs, that each new alias resolves to a file, and that the hub's sibling-nav counters renumber | `hugo-create` + `hugo-design` (`references/hugo-sibling-nav.md`) | none yet | candidate | `integrations.at`, 2026-08-21: two pages moved from `/agentic-engineering/` to `/enterprise-ai/` while two new pages took their hub-card slots. Done by hand: 107 pages, 75 aliases, both retired routes emitting `<meta http-equiv="refresh">` stubs, all six new aliases resolving, counters reading 4/6 and 5/6, 0 dead links across the eight touched pages. Two traps a script would encode: the retired URLs belong to the MOVED file's `aliases:` and must not also be declared on the new page replacing it, because two pages claiming one alias is a build conflict; and a page repointed off a hub card silently loses its prev/next strip, with no build error | none |
| Publish one video file to a named YouTube channel and keep its metadata in the repo: read the OAuth values from secrets or a credentials file at runtime, exchange the refresh token, assert the token owns the expected channel before the insert, validate the title and description limits, resumable-upload with 308 resume, and record the returned id in the page's own front matter so a re-run cannot upload a duplicate | `youtube-data-api` | `.github/scripts/youtube_publish.py` in integrations.at (`insert` / `sync` / `bootstrap` subcommands, `--dry-run`, `--channel`, driven by `youtube-upload.yml`, `youtube-metadata-sync.yml` and `youtube-token-health.yml`) | tested | `integrations.at`, 2026-08-21: two sessions the same evening. The manual one uploaded `assets/youtube/ai-at-work.mp4`, 92 MB, unlisted onto `UCdYy2oc8IwqFBWqZZm_JXBA` as `UP0WrrHfwPw` after the stored token proved dead and the operator re-minted it; a parallel session then turned that path into the CI scripts above. Four traps they encode: the destination channel must be asserted before the insert, because a token binds to whichever of an account's channels was picked at consent; a dead token fails at the token exchange, not at the upload; Python's urllib follows the 308 that carries the resume offset; and the Chrome extension's `file_upload` caps at 10 MB, so a browser is never the fallback for video | `.github/scripts/test_youtube_upload.py`, 18 checks, no credentials and no network: a fake YouTube on localhost serves the resumable protocol and injects one 503, and 8912900 bytes are asserted byte-for-byte across two chunks after the resume; plus the 308-not-followed handler, the four snippet limits, the front-matter round trip (body untouched, cards and faq intact, no duplicate block, block scalar preserved) and all three guards |
| Hugo route rename: inventory, paired folder move, link sweep, verify | `hugo-routing` | `scripts/rename-route.sh` | scripted | integrations.at `/training/` -> `/team-enablement/` (2026-08-21), check direction: all counts 0, redirect correct | - |
| Prove a git-ignored config file and its committed sanitized mirror agree: identical key sets, every sensitive key placeheld in the mirror and its real value absent from the whole file including comments, both files shell-sourceable where that applies, and git ignoring the real one while leaving the mirror committable | `migrate-config` (`SKILL.md` "Hard rules", `references/gotchas.md` 13) | `.claude/skills/migrate-config/scripts/verify-mirrors.sh` (repo-local; `--real`/`--mirror` per pair, `--sensitive`, `--shell`, `--repo`) | scripted | `media-creation-team`, 2026-08-22: written after a hand-scrubbed `.env.deployment.example` passed a password grep and still carried the real SSH key filename, the estate SQL login and three other tenants' logins, which a concurrent session then committed and pushed. Two canonical invocations both exit 0 (`PAIRS=1 KEYMISMATCH=0 LEAKS=0 SOURCEFAIL=0 IGNOREFAIL=0`): the `.env` pair without `--shell` and the `.env.deployment` pair with it. Building it also found a real inconsistency the greps had not, a public GHCR owner placeheld under one key and printed verbatim under two others. Two traps it encodes: a value deliberately identical on both sides is not a leak, so the sensitive set is named rather than inferred from difference; and `--shell` cannot be global, because a compose-parsed `.env` connection string is a valid value and an invalid shell line | none |
| Retire one directory tree into another without losing a byte: hash every file on both sides, bucket each source file as identical-elsewhere, differing or unique, move the unique ones and rescue never-committed ones OUT of the tree entirely, delete only the proven duplicates, then repoint the ignore rules, tooling paths and routing hooks that named the old tree | `claude-learn` (candidate home: `git-history-ops`) | none yet | candidate | `media-creation-team`, 2026-08-22: a 1.3 GB per-video tree folded into the application's media root. 155 of 159 files proven byte-identical by sha256 before deletion, the 4 gitignored never-committed files copied outside the repo first and re-confirmed at their new path, 49 asset references re-resolved with 0 missing. Three traps a script would encode: a folder-level comparison hides the unique file inside an otherwise duplicate folder, so identity is proven per file; rescuing an ignored file to another path inside the same tree rescues nothing; and a relocation that changes directory depth silently breaks relative-path config, so the wrapper folder is chosen to preserve the hop count and the one surviving literal is asserted by resolving it, not by reading it | none |
| House shell contract probe: assert an app's rendered chrome against the shared contract (rail geometry, header height, seam colours, no width caps, token presence) | house-shell (media-creation-team, repo-local) | `.claude/skills/house-shell/scripts/shell-contract-probe.js`, evaluated through the chrome-devtools MCP against a running dev server | scripted | media-creator-ui on 4220, 14/14 pass; video-editor-ui on 4210, caught a real inner-scrollbar inset the first run and the tolerance was widened to 18px. **The 14/14 is stale as of 2026-08-23**: a nav panel column was inserted between the rail and `main` in all three apps, which invalidates the adjacency check `content starts at rail edge` whenever the rail is expanded, and the probe was extended for it but has not been re-run. A probe encodes the layout it was written against, so a shell change silently converts its assertions into failures against correct code; re-run before quoting a count | - |
| Which database is an API actually reading: compare the running process's connection string with `.env`, enumerate `sys.databases` name variants and their row counts before concluding data was lost | dotnet-conventions | not written | candidate | media-creation-team, 2026-08-23: the media library looked empty because `appsettings.json` named `media-creator` while `.env` named `media-creator-db`, and both existed | - |
| Enumerate every inbound reference to a doc path across a whole repo, splitting live control files from historical records that must never be retargeted, and flagging the rename-chain case where the path still exists so each hit is ambiguous between the old and the new occupant | `claude-learn` (`references/claude-learn-distribute.md` step 1) | `~/.claude/skills/claude-learn/scripts/doc-refs.py` | scripted | `media-creation-team`, 2026-08-23: written after a six-task doc rename chain (`architecture.md`->`media-asset-layout.md`, `config.md`->`configuration-management.md`, `local-stack.md`->`architecture.md`) whose hand-run greps reported clean. The script immediately found three survivors they had missed: `.env` and `.env.example` still citing `docs/local-stack.md` in a comment, and `.claude/hooks/claude-md-guard.sh` routing video work to `docs/marcus.md`, a file that has never existed in any commit. Three traps it encodes: a basename match needs a left boundary excluding `.` and `-` or `config.md` matches `use-video-config.md`; `.venv`/`site-packages` swamp the output; and when the freed name has been reused the count means nothing, so it says so instead of reporting zero | none |
| Which process actually serves `http://localhost:<port>` when a container and a natively started dev server are both bound to it: enumerate the listeners per port, classify each owner as a Docker helper or a real application process, cross-reference `docker ps` for the publishing container, and print a per-port verdict plus a shadowed count | `docker-images` (`references/local-host-integration.md`) | `~/.claude/skills/docker-images/scripts/who-owns-port.ps1` | tested | `media-creation-team`, 2026-08-23: a Veo fix verified fine on 5210 while the SPA on 4220 kept serving a stale build. A native `MediaCreatorMcp.exe` held `127.0.0.1`/`::1` on 5210 with `com.docker.backend` on the wildcard, and the `ng serve` everyone assumed was on 4220 was on 4221. Windows treats a specific address and the wildcard as different endpoints, so both bind with no error and localhost silently reaches the native one. Two traps it encodes: `wslrelay` and `com.docker.backend` are both Docker helpers, so a loopback listener is only a native process when the name is the app's; and a port can be published by compose with nothing listening at all | both directions on the live stack: container-only 4220 and 5210 return `shadowed=0` at exit 0; starting a native `dotnet run` on 5210 and re-running returns `SHADOWED: localhost reaches native MediaCreatorMcp (pid 101524) on ::1` at exit 1, and an unused port reports `NOTHING LISTENING` |
| Content-Security-Policy audit of a repo's SPA container configs: derive each SPA's actually-called origins from its own environment files, then assert every directive that governs a browser-issued load covers them, not just `connect-src` | `docker-images` / `e2e-playwright` | none yet | candidate | `media-creation-team`, 2026-08-23: two of five defects in a container test leg were this, and neither could appear under `ng serve` because the CSP lives only in the image's `nginx.conf`. `media-creator-ui` allowed the API origin in `connect-src` but not `img-src`, and declared no `media-src` at all so it fell back to `default-src 'self'`; `article-writer-ui` had the identical hole, unexercised by any spec; `video-editor-ui` was already correct. Measured before and after on the live grid: 428 requests all stalled and 0 of 214 images decoded, versus 56 requests and 20 of 20 in-viewport images decoded. Two traps it would encode: the diagnostic signature is an `<img>` with `complete === true` and `naturalWidth === 0` while `fetch()` of the same URL returns 200 with correct bytes, which reads like network saturation and is not; and `connect-src` passing is exactly what makes the other directives look fine, since every hand check reaches for `fetch` first | none |
| Derive a copy-length budget from a live element instead of guessing it: substitute strings of known length into the target node through the browser, read back the rendered line count and the ancestor's height at each step, and return the character RANGE that holds a given line count rather than a single ceiling | `hugo-design` (`SKILL.md` standing preferences) | none yet | candidate | `integrations.at`, 2026-08-23: aligning `/ai-trends/` to two sibling hubs needed a banner description that rendered exactly three lines, because 2 lines gave a 213px banner and 4 gave 279px against the siblings' 246px. Substitution found the band precisely: `<=134` two lines, `136-198` three lines, `>=202` four. The same run got the sibling budget wrong by eye and paid a round trip for it, capping an `intro_title` at 85 characters measured on a page whose h2 sits in a 1091px column when the target column was 727px, where the real one-line threshold is 81. Two traps it would encode: the answer is a range and a bare ceiling is wrong in both directions, since undershooting breaks a height match as surely as overshooting; and the measurement is only valid on the element that will hold the text, never on a sibling page rendering the same class in a different container | none |
| Set a workstation up as a SQL client for an engagement: install `ms-mssql.mssql`, register every database as a grouped VS Code connection profile, and persist the passwords to the OS credential store rather than to `settings.json` | `sql-server` (`references/vscode-mssql-profiles.md`) | `install-db-extension.ps1` (bauer-sport repo root) | scripted | run end to end against a scratch `settings.json`, 2026-08-26: 3 groups and 6 Hetzner + 35 local profiles written, unrelated keys and JSONC comments survived the parse, and `cmdkey /list` returned one `Microsoft.SqlTools|...` target per Hetzner database | none |
| Stand a .NET API + Angular SPA pair up as a local compose stack against the host's SQL Server: Dockerfile and nginx for the SPA, a same-origin `/api/` proxy, a build configuration whose environment points at that relative base, a dedicated SQL login with the table roles plus `GRANT EXECUTE` on every function the projections call, an env-driven compose file, and a bring-up verified on temporary host ports because the developer's dev servers hold the real ones | `docker-images` (`references/local-host-integration.md`) | none yet | candidate | `integrations.at`, 2026-08-29: `secrets-mcp` + `secrets-ui` as the `secrets-team` stack. Done by hand once, end to end, and every step was mechanical given (project dir, host port, database, login). Three traps it would encode: `proxy_pass` must carry no path when the API's own routes already start with `/api`, a second `-f` override concatenates `ports:` instead of replacing it so the verification run binds the live dev ports too, and `db_datareader` leaves a scalar decryption function unexecutable so exactly one endpoint 500s while the health check stays green | none |
| Prove a list's column geometry across the full matrix of viewports times collapsible-rail states: for each cell resize, toggle the rail through its own button, then read back the resolved `grid-template-columns`, the flexible column's rendered width, the line count of the longest name and longest URL, and the document's horizontal overflow | `angular-conventions` (`references/angular-table-column-layout.md`) | none yet | candidate | `integrations.at`, 2026-08-29: the secrets list. Two orientations times two rail states is four cells per surface and eight with the detail view, each one four `evaluate_script` reads plus a resize plus a click, run twice (before and after the container-query conversion) to prove the conversion regressed nothing. Done by hand it cost several agent round trips and the agent measured only the default rail state on the first pass, which is exactly the omission a script cannot make. Three traps it would encode: the rail must be toggled by clicking its real control, since writing the store bypasses the rendered state under test; the threshold to re-assert after a media-to-container conversion is the container's width at the OLD viewport breakpoint, not the breakpoint number itself; and a devtools-docked window reports an `innerWidth` scaled off the requested size and floors around 557px, so the achieved width must be read back rather than assumed | none |
| Move a numbered course tree onto a new outline without losing a byte: validate the whole crosswalk before touching anything, move every entry to a unique temp name and only then to its final one so a swap or rotation cannot collide, recover from Windows build locks with a clear-and-retry loop, prune the directories the plan emptied, and print a content-loss receipt (tracked-file count and sorted blob hash before versus after, plus the deletion count) | `create-class` (`references/restructure-execute.md`) | `~/.claude/skills/create-class/scripts/renumber-tree.sh` | scripted | `agentic-sw-engineering`, 2026-08-30: the metro.md renumbering, 30 folder moves including a 03/04 swap and a two-module merge into one split parent. The real run was done by hand and is what the script encodes; the script itself was proven on a throwaway repo reproducing both shapes (`moved=4 retries=0 deletions=0 content=PASS`) and on an invalid plan (missing source plus duplicate destination refused at exit 1 with nothing moved). Three traps it encodes: a naive single pass cannot swap two numbers; deleting the locked `obj` once is not enough because a watcher re-acquires it within seconds, so the delete and the move must be a retry loop; and the folder that reports the lock is never the one holding it, it is `obj/Debug` several levels down | none |
| Verify a markdown tree after a restructure: resolve every internal link against its own file's directory (root-anchored `/...` against the repo root), then assert every numbered sibling set runs 01..NN with no gap or duplicate and that each numbered folder carries a readme | `create-class` (`references/restructure-execute.md`) | `~/.claude/skills/create-class/scripts/link-check.sh` | scripted | `agentic-sw-engineering`, 2026-08-30: run over `demos/` after the renumbering it returned `files=113 links=298 broken=3 prefix_defects=0 missing_readme=0`, reproducing the hand audit of a verification agent exactly, same three unfixable targets (deleted `.github` agents and skills, not renamed ones). The prefix and readme checks are what catch the failure mode a link check alone misses: a tree where every link resolves because a whole numbered slot was silently skipped | none |
| Repoint every VS Code agent / chat mode file in a folder at one model, rewriting or inserting the frontmatter `model:` line | `copilot-model` | `scripts/set-agent-model.py` | scripted | `agentic-sw-engineering/.github/agents`: 13 files replaced=13, and a two-hop round-trip on a scratch copy came back byte-identical to the hand-edited tree | none |
