[project]
name = "doubler-api"
version = "0.1.0"
description = "A simple API that returns the double of an input value"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.34.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
