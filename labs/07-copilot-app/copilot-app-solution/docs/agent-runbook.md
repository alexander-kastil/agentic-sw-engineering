# Agent Runbook

Settings this session ran with when it produced the `Fresh Food, Fast` heading change in `src/food-app/food-shop`.

| Setting | Value |
|---|---|
| Model | Claude Sonnet 4.5 |
| Reasoning effort | Low |
| Permission mode | assisted |
| Worktree | New worktree (isolated from the main checkout) |

A teammate reproducing this run should open a new session against their own fork, pick **New worktree**, set the model and reasoning effort above in the composer menu before sending the prompt, and confirm `/permissions show` reports `assisted`.
