# Delegation Test

This page converts temperatures between Celsius and Fahrenheit. Type a number in either field and the other field updates automatically.

## How to open

Double-click `index.html`, or from the repo root run:

```powershell
start src/scratch/delegation-test/index.html
```
