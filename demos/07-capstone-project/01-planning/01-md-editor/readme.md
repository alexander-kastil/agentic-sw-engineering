## Scaffold a React Markdown Editor

```prompt
Create a basic markdown editor in React with the following features:
- Use react hooks
- Create state for markdown with default text "type markdown here"
- A text area where users can write markdown
- Show a live preview of the markdown text as I type
- Support for basic markdown syntax like headers, bold, italics
- Use React markdown npm package
- The markdown text and resulting HTML should be saved in the component's state and updated in real time
- The markdown editor should support switching between edit and preview modes with a toggle button
- Use this free markdown editor: https://github.com/uiwjs/react-md-editor?tab=readme-ov-file
```

## Scaffold an Angular Markdown Editor

```prompt
Create a basic markdown editor in Angular with the following features:
- Use Angular components and services
- Create state for markdown with default text "type markdown here"
- A text area where users can write markdown
- Show a live preview of the markdown text as I type
- Support for basic markdown syntax like headers, bold, italics
- Use Angular markdown npm package
- The markdown text and resulting HTML should be saved in the component's state and updated in real time
- The markdown editor should support switching between edit and preview modes with a toggle button
- Use this free markdown editor: https://github.com/jfcere/ngx-markdown
```
